= turbo_frame_tag 'content' do
  %section.wrap
    .card-wrap
      .item-card
        .card-head
          %h1 Арбитражи по чеку
        = render "#{role_namespace}/payments/table", payments: @payments

        .card-wrap
          = render 'shared/pagination', pagy: @pagy
