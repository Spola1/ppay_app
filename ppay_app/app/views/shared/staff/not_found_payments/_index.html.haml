%section.wrap
  .card-wrap
    .item-card
      .card-head
        %h1 Реестр ненайденных платежей

      .card-wrap
        = render 'shared/staff/not_found_payments/table'

      .card-wrap
      = render 'shared/pagination', pagy: @pagy