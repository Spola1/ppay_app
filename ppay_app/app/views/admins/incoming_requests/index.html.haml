%section.wrap
  .card-wrap
    .item-card
      %h1.mb-4.text-xl Входящие запросы

      - if @incoming_requests.present?
        %table.items-list-table
          %thead.item-list-thead
            %tr
              %th.items-list-th ID
              %th.items-list-th создано
              %th.items-list-th приложение
              %th.items-list-th тип

              %th.items-list-th

          %tbody
            - @incoming_requests.each do |ir|
              %tr.bg-white.border-b
                %td.items-list-td-link= link_to ir.id, ir
                %td.items-list-td= ir.decorate.formatted_created_at
                %td.items-list-td= ir.app
                %td.items-list-td= ir.request_type

                %td.items-list-td.px-1.w-0
                  = link_to ir do
                    .icon-button-gray
                      = fa_icon 'arrow-right'
      - else
        .empty-list-text
          Запросов пока нет
      .card-wrap
        = render 'shared/pagination', pagy: @pagy
