= turbo_frame_tag 'payments' do
  %section.wrap
    .card-wrap
      .item-card
        %h1.mb-4.text-xl Платежи
        = render 'table', payments: @payments

        .card-wrap
          = render 'shared/pagination', pagy: @pagy