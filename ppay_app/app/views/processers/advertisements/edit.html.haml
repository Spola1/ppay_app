%section.wrap
  .card-wrap
    .item-card
      .flex.mb-4.items-center
        .text-xl
          Редактировать объявление
          = "(ID: #{@advertisement.id})"
      .mt-8.my-4
        = render 'shared/staff/advertisements/form'
      .back-link
        = link_to '<- К списку объявлений', advertisements_path
