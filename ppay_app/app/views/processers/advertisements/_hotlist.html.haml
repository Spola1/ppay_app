= turbo_frame_tag "advertisement_#{advertisement.id}_hotlist" do
  = link_to payment_path(payment), data: { turbo: false }, class: 'block' do
    = render 'processers/advertisements/hotlist_payment', payment: