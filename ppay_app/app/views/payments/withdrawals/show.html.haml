.show_content{:class => "show_withdrawal_block"} 
  = render 'show'

  - if @payment.arbitration
    = render "/shared/tg_link"

  .uuid_payment_block
    uuid: 
    = @payment.uuid
    - if user_signed_in?
      = "(payment_status: \"#{@payment.reload.payment_status}\")"
  .clear