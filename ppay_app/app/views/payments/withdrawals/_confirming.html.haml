%p Вы получили деньги?

= form_for payment, url: payments_deposit_path(uuid: payment.uuid, signature:), action: :update,
  data: { turbo_confirm: 'Вы уверены, что полностью получили платёж и хотите его подтвердить?' } do |f|
  = hidden_field_tag :event, 'confirm'
  = f.submit 'Подтвердить', confirm: 'Вы уверены, что хотите отменить платёж?', class: "text-gray-400 underline"
