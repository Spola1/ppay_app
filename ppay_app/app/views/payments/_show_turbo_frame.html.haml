= turbo_frame_tag "payment_#{payment.uuid}", 'data-controller': 'countdown' do
  = render payment_status_partial(payment), payment:, signature:

  - if payment.arbitration
    = render "/shared/tg_link"
