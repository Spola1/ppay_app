.proc-head
  .proc-logo
  .proc-text
    UID Check

.proc-amount-block
  .proc-amount-content
    = "#{payment.national_currency_amount} #{payment.national_currency}"


.proc-shop-block
  .proc-shop-title
    Магазин
  .proc-shop-content
    = payment.merchant.name

.mt-1
  .proc-order-block
    .proc-order-title
      Заказ
    .proc-order-content
      = payment.external_order_id

= render 'payments/errors', payment: payment

.simple-form-for-payment-deposit
  = simple_form_for payment, url: payments_statuses_deposit_path(uuid: payment.uuid, signature:), action: :update do |f|
    .select-payment-system
      = f.label :payment_system, 'Выберите платежную систему'
    
    = f.select :payment_system, [["Выбрать", ""], "Sberbank", "Tinkoff", "Raiffeisen", "AlfaBank"], {}, class: "grey-drop-down"
              
    
    = hidden_field_tag :event, 'search' 
    
    = f.submit "Подтвердить", class: "green-button-submit mt-3"
    
.please-pay-from-bank 
  Пожалуйста, выбирайте из списка только тот банк, карта которого у Вас есть и с которой Вы планируете  оплатить!

= render 'payments/cancel_form', payment:, signature:, url: payments_statuses_deposit_path(uuid: payment.uuid, signature:)

.alerts-block
  .text-red.font-semibold
    Внимание:
  .my-0.5 
    1) при отправке денежных средств писать сообщения в приложении категорически запрещено.
  .my-4
    2) платеж должен быть выполнен в точности на указанную сумму, включая все знаки после запятой.
  .my-4 
    3) средства необходимо отправлять строго внутрибанковским переводом, банк отправитель и банк получатель должны быть одинаковыми. В противном случае зачисление оплаты не гарантируется.
